# RTZR (Return Zero / vito.ai) STT API credentials.
# Copy this file to `.env` and fill in your own values — never commit `.env`.
# `rtzr configure` writes these into a local config file outside the repo instead;
# environment variables (if set) take priority over that local config.

RTZR_CLIENT_ID=
RTZR_CLIENT_SECRET=
