{
  "name": "@spencer0124/rtzr-mcp",
  "version": "0.2.0",
  "description": "Remote MCP server (Cloudflare Workers) for the RTZR (Return Zero) STT API — BYO-key, wraps @spencer0124/rtzr-core.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@spencer0124/rtzr-core": "workspace:*",
    "agents": "^0.17.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260713.1",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9",
    "wrangler": "^4.110.0"
  }
}
