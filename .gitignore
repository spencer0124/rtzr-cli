# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# wrangler (Cloudflare Workers) local dev cache
.wrangler/

# secrets — never commit real keys
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

# local CLI config (rtzr configure writes outside the repo, but just in case)
.rtzr/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store

# editor
.vscode/
.idea/

# test/coverage
coverage/

# internal-only docs — concept.md itself says not to publish it,
# guide_for_work.pdf/company_introduction.pdf are the intern assignment
# materials, and rtzr-api-raw.md is a scrape of RTZR's official docs.
# Public docs live in docs/ (committed).
internal-docs/

# scratch artifacts from live API test runs — not curated examples
out/
out_diarized/
out_test/
test_audio/
