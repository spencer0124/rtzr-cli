{
  "name": "rtzr-cli-monorepo",
  "private": true,
  "packageManager": "pnpm@11.13.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "dev": "pnpm --filter @seungyongcho/rtzr-cli run dev"
  }
}
